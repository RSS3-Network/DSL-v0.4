package alchemy_test

import (
	"testing"
)

func TestNewClient(t *testing.T) {
	// TODO RPC endpoints need to verify the API key
	//polygonClient, err := alchemy.NewClient(protocol.NetworkPolygon, "")
	//if err != nil {
	//	t.Error(err)
	//}
	//
	//result, err := polygonClient.GetAssetTransfers(context.Background(), alchemy.GetAssetTransfersParameter{
	//	Category: []string{
	//		alchemy.CategoryExternal,
	//	},
	//})
	//if err != nil {
	//	t.Error(err)
	//}
	//
	//for _, transfer := range result.Transfers {
	//	t.Log(transfer)
	//}
}
